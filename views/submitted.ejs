<!DOCTYPE html>
<html lang='en'>
    <head>
        <title>Welcome!</title>
    </head>
    <body>
        <h1>Hello <%= name %>!</h1>
        <% if (name !== 'admin') { %>
            <h2>Your password is <%= md5(password) %>.</h2>
        <% } else { %>
            <h2>Sh... Your password is hidden.</h2>
        <% } %> 
    </body>
</html>